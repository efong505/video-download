# AWS Profile Rule

This project uses the `ekewaka` AWS profile (account 371751795928).

- ALWAYS use `--profile ekewaka` with all AWS CLI commands
- ALWAYS set `AWS_PROFILE=ekewaka` before running any Python/boto3 scripts
- NEVER use the `default` profile — it points to a different account (372110294325)
